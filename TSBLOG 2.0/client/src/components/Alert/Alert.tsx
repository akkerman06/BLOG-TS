import React from 'react';
import cls from './Alert.module.scss'
const Alert = () => {
    return (
        <div className={cls.auth}>
            Alert
        </div>
    );
};

export default Alert;
